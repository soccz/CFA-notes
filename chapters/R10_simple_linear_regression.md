# Reading 10: Simple Linear Regression


## LOS 10.a: 단순 선형 회귀 모형

### 상황 속으로

당신이 신발 가게 사장이라고 합시다. 매일 앞을 지나가는 사람 수와 매출 사이에 관계가 있을 것 같다. 지난 5일 데이터를 모았다:

| 통행량(명) | 매출(만 원) |
|-----------|-----------|
| 100 | 50 |
| 200 | 90 |
| 150 | 70 |
| 300 | 130 |
| 250 | 120 |

통행량이 400명이면 매출이 얼마일까? "대충 170만 원쯤?" — 이 "대충"을 **수식으로 정확하게** 만드는 도구가 회귀분석이다.

R03의 상관계수는 "통행량이 많으면 매출도 높다(양의 관계)"라는 **방향**만 알려줬다. 회귀분석은 한 걸음 더 나아간다: "통행량이 1명 늘면 매출이 약 0.45만 원 늘어난다"처럼 **정량적 관계**를 수식으로 표현하고, 통계적으로 유의한지 검정하고, 미래 값을 예측한다.

### 숫자를 직접 넣어보자

위 데이터에서 평균을 구하면 $\bar{X} = 200$, $\bar{Y} = 92$이다. 각 점에서 평균까지의 차이를 구해 Cov와 Var를 계산하면:

- Cov(X, Y) = 2,200
- Var(X) = 5,000

기울기 = 2,200 / 5,000 = **0.44**
절편 = 92 - 0.44 × 200 = **4**

회귀방정식: $\hat{Y} = 4 + 0.44X$

통행량 400명이면? $\hat{Y} = 4 + 0.44 \times 400 = 180$만 원.
통행량 50명이면? $\hat{Y} = 4 + 0.44 \times 50 = 26$만 원.

**왜 이런 결과가 나올까?** 기울기 0.44는 "통행량 1명 증가 시 매출 0.44만 원(4,400원) 증가"를 의미한다. 절편 4는 통행량이 0명이어도 기본 매출이 4만 원이라는 것 — 아마 단골 매출일 것이다.

### 이제 정리하면

$$Y_i = b_0 + b_1 X_i + \varepsilon_i$$

- $b_0$ = 절편 (X=0일 때 Y의 예측값)
- $b_1$ = 기울기 (X가 1단위 변할 때 Y의 예상 변화량)

### 최소제곱법 (OLS)

잔차 제곱합(SSE)을 최소화하는 직선을 찾는 방법이다:

$$\hat{b}_1 = \frac{\text{Cov}(X, Y)}{\text{Var}(X)}, \quad \hat{b}_0 = \bar{Y} - \hat{b}_1 \bar{X}$$

**왜 기울기가 Cov/Var인가?** X의 변동성(Var) 중에서 Y와 함께 움직이는 부분(Cov)만 추출한 것이다. 기울기와 상관계수의 관계: $\hat{b}_1 = \rho_{XY} \cdot \sigma_Y / \sigma_X$. 회귀선은 반드시 점 $(\bar{X}, \bar{Y})$를 통과한다.

**일상 상황으로 이해하기:** "키가 클수록 몸무게가 무겁다"는 상식이 있다. 키(X)가 1cm 늘 때 몸무게(Y)가 몇 kg 늘어나는가? 이것이 기울기다. 키가 0cm인 사람의 몸무게(절편)는 현실적으로 의미가 없지만, 수학적으로는 직선의 Y축 절편이다. "절편이 항상 현실적 의미를 갖는 것은 아니다"는 점을 기억하자.

### 만약 변수를 바꿔보면?

위 신발 가게에서 통행량 대신 **기온**을 독립변수로 쓰면? 여름에 사람들이 더 많이 돌아다니므로 기온과 매출 사이에도 관계가 있을 수 있다. 하지만 기울기는 완전히 달라질 것이고, 어떤 변수가 더 좋은 예측을 주는지는 R²로 비교한다.

### 베타 계산 예제

친구가 S&P 500 지수와 ABC 주식의 관계를 알고 싶어 한다. 데이터를 모아보니:

- Cov(S&P, ABC) = 0.000336, Var(S&P) = 0.000522

$\hat{b}_1 = 0.000336/0.000522 = 0.64$
$\hat{b}_0 = -4.05\% - 0.64 \times (-2.70\%) = -2.3\%$

회귀방정식: $\hat{Y}_{ABC} = -2.3\% + 0.64 \times X_{S\&P}$

S&P가 10% 오르면? ABC는 $-2.3 + 0.64 \times 10 = 4.1\%$ 오른다.
S&P가 10% 내리면? ABC는 $-2.3 + 0.64 \times (-10) = -8.7\%$ 내린다.

**왜 이런 결과가 나올까?** 기울기 0.64가 바로 ABC 주식의 **베타(β)**다. β < 1이므로 시장보다 덜 움직인다 — 시장이 10% 올라도 6.4%만 올라간다. 방어적 주식이다.

**변수를 바꿔보면?** 만약 β = 1.5인 공격적 주식이라면? S&P가 10% 오를 때 이 주식은 $b_0 + 1.5 \times 10 = 약 15\%$ 오른다. 하지만 10% 내릴 때도 약 15% 내린다. "높은 베타 = 높은 수익 잠재력 + 높은 손실 위험"이다.

**일상 상황:** 지진이 나면 고층 건물이 저층보다 더 크게 흔들린다. 베타가 높은 주식은 "고층 건물"이고, 베타가 낮은 주식은 "저층 건물"이다. 호황(약한 지진)에는 고층이 좋지만, 폭락(강한 지진)에는 저층이 안전하다.

### 잔차란 무엇인가? — 숫자로 직접 느끼기

**일상 상황:** 키로 몸무게를 예측하는 모형이 "키 170cm → 몸무게 65kg"이라고 예측했다. 실제로는 75kg이다. 차이 10kg이 잔차다. 이 사람은 "키 대비 뚱뚱하다"는 의미다.

신발 가게 데이터에서 잔차를 직접 계산하면:

| 통행량 | 실제 매출 | 예측값 ($4 + 0.44X$) | 잔차 |
|-------|---------|-------------------|------|
| 100 | 50 | 48 | **+2** |
| 200 | 90 | 92 | **-2** |
| 150 | 70 | 70 | **0** |
| 300 | 130 | 136 | **-6** |
| 250 | 120 | 114 | **+6** |

**왜 잔차가 중요한가?** 잔차의 패턴을 보면 모형이 적절한지 판단할 수 있다. 잔차가 랜덤하면 좋은 모형, 패턴이 있으면 모형이 뭔가를 놓치고 있다는 신호다.

**만약 통행량이 높을수록 잔차가 커진다면?** 등분산성 위반이다. 통행량이 많은 날은 예측이 부정확하다는 뜻 — 아마 통행량이 많은 날에는 다른 요인(날씨, 행사)의 영향이 커지기 때문일 것이다.

**잔차의 합은 항상 0이다.** 위 표를 확인하면: +2 + (-2) + 0 + (-6) + (+6) = 0. 이것은 OLS의 수학적 성질이다 — 회귀선이 $(\bar{X}, \bar{Y})$를 통과하므로, 양의 잔차와 음의 잔차가 상쇄된다. "잔차 합 = 0"이라고 해서 모형이 완벽하다는 뜻이 아니다 — 각 잔차의 크기를 봐야 한다.


## LOS 10.b: 회귀 분석 가정

상상해보세요. 회귀선을 그었는데 데이터 점들이 왼쪽에선 직선 근처에 있고 오른쪽에선 멀리 퍼져 있다면? 직선 하나로 요약하기에 뭔가 이상하다. 이런 문제를 막기 위해 4가지 가정이 필요하다:

1. **선형 관계:** X와 Y 사이에 직선 관계. 곡선이면 직선 모형이 엉터리 예측을 한다.
2. **등분산성 (Homoskedasticity):** 잔차 분산이 모든 관측치에서 일정. 위에서 말한 "오른쪽에서 퍼지는" 현상이 없어야 한다.
3. **독립성:** 잔차 간 상관 없음. 오늘 잔차가 크다고 내일도 큰 건 안 된다.
4. **정규성:** 잔차가 정규분포. 큰 표본이면 CLT에 의해 덜 중요해진다.

### 가정 위반 시 무슨 일이 벌어지는가?

**숫자로 직접 확인해봅시다.**

**이분산성(Heteroskedasticity)이 있으면:**

**일상 상황:** 소득으로 지출을 예측한다. 저소득자는 대부분 수입의 대부분을 쓴다(잔차 작음). 고소득자는 아끼는 사람과 호화 소비하는 사람이 극단적이다(잔차 큼). 소득이 높을수록 잔차가 퍼진다.

결과: 회귀 계수 자체는 여전히 편향되지 않지만, **표준오차가 잘못 계산된다.** t-검정 결과를 신뢰할 수 없게 된다. 유의하지 않은 변수를 유의하다고 판단하거나, 유의한 변수를 놓칠 수 있다.

**자기상관(Serial Correlation)이 있으면:**

**일상 상황:** 오늘 교통 체증이 심했다면 내일도 심할 가능성이 높다(도로 공사 때문). 잔차가 독립이 아니다.

결과: 이분산성과 마찬가지로 표준오차가 왜곡되고, 특히 **과소추정**되는 경향이 있다. 기울기가 유의하지 않은데 유의하다고 잘못 결론 내릴 위험이 크다.

**비선형 관계가 있으면:**

**숫자 예시:** 광고비(X)와 매출(Y)의 관계가 실제로는 로그 함수인데 직선으로 모형을 만들면 어떻게 될까? 광고비 100만 원 구간에서는 매출이 급격히 오르지만, 1,000만 원 구간에서는 완만하게 오른다(수확체감). 직선은 100만 원 구간에서 과소예측, 500만 원 구간에서 과대예측, 1,000만 원 구간에서 다시 과소예측한다. 잔차에 U자 패턴이 나타난다 — 이것이 "선형성 가정 위반"의 시각적 증거다.

**정규성 가정이 위반되면:**

**일상 상황:** 주식 수익률은 정규분포를 따르지 않는다 — "두꺼운 꼬리(fat tails)"가 있다. 극단적 폭락이 정규분포 예측보다 훨씬 자주 발생한다. 잔차가 비정규이면 신뢰구간과 가설검정의 정확도가 떨어진다. 다만 표본이 크면(n > 30) CLT에 의해 검정통계량이 근사적으로 정규분포를 따르므로 영향이 줄어든다.


## Module Quiz 10.1

**Q1.** 기울기 10.0의 해석?
**정답: B** — 독립변수 1단위 변화 시 종속변수 10단위 변화 예측

**Q2.** 필수 가정이 아닌 것?
**정답: C** — 종속변수가 잔차와 무관해야 하는 게 아니라, **독립변수**가 잔차와 무관해야 함


## LOS 10.c~d: 적합도 측정, ANOVA

### 상황 속으로

당신이 두 개의 회귀 모형을 만들었다고 합시다:
- 모형 A: 통행량으로 매출 예측
- 모형 B: 날씨(맑음/흐림)로 매출 예측

둘 다 직선을 그을 수 있지만, 어느 것이 매출을 더 잘 설명하는가? 이것을 측정하는 도구가 R²와 ANOVA다.

### 변동의 분해 — 숫자로 느끼기

신발 가게 5일 데이터에서 평균 매출이 92만 원이다. 3일째 실제 매출은 70만 원이었다.

- 평균과의 차이(총 변동): 70 - 92 = -22
- 모형 예측값: $\hat{Y} = 4 + 0.44 \times 150 = 70$ → 예측과 평균의 차이(설명된 변동): 70 - 92 = -22
- 실제와 예측의 차이(잔차): 70 - 70 = 0

모든 점에서 이런 분해를 하면:

$$\text{SST} = \text{SSR} + \text{SSE}$$

| 기호 | 이름 | 의미 |
|------|------|------|
| SST | 총 제곱합 | $\sum(Y_i - \bar{Y})^2$ |
| SSR | 회귀 제곱합 | $\sum(\hat{Y}_i - \bar{Y})^2$ — 설명된 변동 |
| SSE | 오차 제곱합 | $\sum(Y_i - \hat{Y}_i)^2$ — 설명되지 않은 변동 |

**일상 상황으로 이해하기:** 학생들의 시험 성적 차이(SST)를 설명하려 한다. 공부 시간(X)으로 설명되는 부분(SSR)과, 컨디션·운 등 설명 안 되는 부분(SSE)으로 나뉜다. R²가 0.85면 "공부 시간이 성적 차이의 85%를 설명한다." 나머지 15%는 다른 요인이다.

### 결정계수 (R²) — 두 모형 비교

$$R^2 = \frac{\text{SSR}}{\text{SST}}$$

모형 A의 R² = 0.85 → 통행량이 매출 변동의 85%를 설명한다.
모형 B의 R² = 0.30 → 날씨는 30%만 설명한다.

**왜 이런 결과가 나올까?** 매출은 주로 얼마나 많은 사람이 지나가느냐에 달려 있지, 단순히 날씨만으로 결정되지 않기 때문이다. 단순 회귀에서 $R^2 = r^2$ (상관계수의 제곱)이다.

**만약 독립변수를 "통행량 + 날씨" 둘 다 넣으면?** R²가 올라갈 수 있다 — 하지만 그건 다중 회귀의 영역이다.

**숫자로 느끼기 — R²의 직관적 의미:** R² = 0.85이면, 데이터 점들이 회귀선에 상당히 가깝게 모여 있다. R² = 0.30이면 데이터 점들이 회귀선에서 많이 흩어져 있다. R² = 0이면 직선이 아무것도 설명하지 못한다 — 수평선(평균)만큼이나 쓸모없다.

### 추정 표준오차 (SEE)

$$\text{SEE} = \sqrt{\text{SSE}/(n-2)}$$

낮을수록 모델의 예측이 실제값에 가깝다.

**일상 상황:** 네비게이션이 "도착 예정 시각 5시"라고 한다. SEE가 2분이면 4:58~5:02 사이에 도착할 가능성이 높다. SEE가 30분이면 4:30~5:30 — 거의 쓸모없다. SEE가 작을수록 네비게이션(모형)이 정확하다.

### F-통계량과 기울기 t-검정

$$F = \frac{\text{MSR}}{\text{MSE}} = \frac{\text{SSR}/k}{\text{SSE}/(n-k-1)}$$

단순 회귀에서 F-검정 = 기울기 t-검정의 동치.

$$t = \frac{\hat{b}_1}{s_{\hat{b}_1}}, \quad df = n-2$$

### 숫자를 넣어보자

S&P-ABC 회귀에서 n=36, $\hat{b}_1=0.64$, $s_{\hat{b}_1}=0.26$

$t = 0.64/0.26 = 2.46$

임계값(df=34, 5% 유의수준) ≈ 2.03 → 2.46 > 2.03 → 기각. 기울기가 통계적으로 유의하다.

**만약 표본이 10개뿐이었다면?** df=8이고 임계값이 약 2.31로 올라간다. 여전히 기각이지만, 표본이 더 작았다면 기각 못 했을 수도 있다. 표본 크기가 중요하다.

**왜 표본이 작으면 기각하기 어려운가?** 작은 표본에서는 "이 관계가 우연일 가능성"을 완전히 배제하기 어렵다. 동전을 5번 던져 4번 앞면이 나온 것(우연 가능)과 500번 던져 400번 앞면이 나온 것(거의 확실히 편향)을 생각해보라.

ANOVA 예제(n=36): $R^2 = 0.0076/0.0482 = 15.8\%$, SEE = 0.035, F = 6.33 > 4.1 → 기각


## Module Quiz 10.2

**Q1.** 자유도: **정답 A** — 모수 추정·예측 모두 n-2

**Q2.** 절편 유의성 검정 대립가설: **정답 A** — Hₐ: a₁ ≠ 0

**Q3.** 독립변수로 설명되는 변동: **정답 C. SSR**


## LOS 10.e: 예측값과 예측 구간

### 상황 속으로

당신이 투자 보고서에 "S&P가 10% 오르면 ABC는 4.1% 오를 것"이라고 쓰려고 한다. 상사가 묻는다: "정확히 4.1%인가? 얼마나 확신하나?"

점추정: $\hat{Y} = \hat{b}_0 + \hat{b}_1 X_p$

S&P 10% 예측 시: $\hat{Y} = -2.3\% + 0.64 \times 10\% = 4.1\%$

하지만 이건 "최선의 추측"이다. 실제로는 범위를 줘야 한다:

예측 신뢰구간: $\hat{Y} \pm t_c \times s_f$

95% 구간(df=34, $s_f$=3.67%): $4.1\% \pm 2.03 \times 3.67\% = -3.4\% \sim 11.6\%$

**왜 이런 결과가 나올까?** 범위가 -3.4%에서 11.6%로 꽤 넓다. 이유는 두 가지: (1) R²가 15.8%밖에 안 돼서 모형의 설명력이 낮고, (2) 표본이 36개로 크지 않다.

**만약 R²가 90%인 모형이었다면?** $s_f$가 훨씬 작아져서 구간이 좁아진다. 예를 들어 $\pm 1\%$ 수준이 될 수 있다. 모형의 설명력이 예측 정밀도를 결정한다.

**일상 상황:** 일기예보가 "내일 기온 20도"라고 하면, ±1도 수준이면 신뢰도 높은 예보, ±10도이면 쓸모없는 예보다. 예측 구간은 "이 예보가 얼마나 믿을 만한가"를 수치로 보여주는 것이다.

### 예측 구간이 넓어지는 3가지 이유

**왜 어떤 예측은 정확하고 어떤 예측은 부정확한가?**

숫자로 직접 확인해봅시다. 예측 오차의 분산은 세 가지 요소의 합입니다:

$$s_f^2 = \text{SEE}^2 \left[1 + \frac{1}{n} + \frac{(X_p - \bar{X})^2}{\sum(X_i-\bar{X})^2}\right]$$

1. **$1$**: 개별 관측치의 고유 변동 (줄일 수 없음)
2. **$1/n$**: 표본이 작으면 회귀선의 위치 자체가 불확실
3. **$(X_p - \bar{X})^2/\sum(X_i-\bar{X})^2$**: 예측하려는 X가 데이터 평균에서 멀수록 불확실

**일상 상황으로 느끼기:** 서울 기온으로 아이스크림 판매를 예측한다. 20~30°C 데이터로 회귀선을 만들었다. 25°C(데이터 중심)에서 예측하면 구간이 좁다. 40°C(데이터 범위 밖)에서 예측하면? 구간이 매우 넓다 — 외삽의 위험이다.

**만약 데이터 범위 밖을 예측하면?** 직선 관계가 유지된다는 보장이 없다. 기온이 45°C면 아이스크림을 사러 나가지 않을 수도 있다 — 관계가 비선형으로 바뀐다.

**숫자 예시 — 3번째 요소의 위력:** $\bar{X} = 200$이고 $\sum(X_i - \bar{X})^2 = 20,000$인 데이터에서:
- $X_p = 200$ (평균과 같음): 세 번째 항 = 0 → 최소 불확실성
- $X_p = 400$ (평균에서 200 떨어짐): 세 번째 항 = $200^2/20{,}000 = 2.0$ → 불확실성 크게 증가
- $X_p = 600$ (평균에서 400 떨어짐): 세 번째 항 = $400^2/20{,}000 = 8.0$ → 불확실성 폭발

데이터 중심에서 멀어질수록 예측 구간이 **기하급수적으로** 넓어진다.


## LOS 10.f: 다양한 함수형태

### 상황 속으로

친구가 "광고비를 2배로 늘리면 매출도 2배가 돼?"라고 묻는다. 현실에서는 광고비를 10배 늘려도 매출이 10배가 되진 않는다 — 수확체감이 있다. 이런 비선형 관계를 다루려면 로그 변환이 필요하다.

숫자를 넣어보자:

- 회사 A: 광고비 100만 → 매출 500만. 광고비 200만 → 매출 700만 (100% 증가에 40% 증가)
- 회사 B: 광고비 100만 → 매출 500만. 광고비 200만 → 매출 1,000만 (100% 증가에 100% 증가)

회사 A는 수확체감, 회사 B는 정비례. 어떤 모형이 A에 적합한가?

| 모형 | 종속 | 독립 | 기울기 해석 |
|------|-----|------|-----------|
| Log-lin | $\ln(Y)$ | $X$ | X 1단위 변화 시 Y의 **상대적 변화율** |
| Lin-log | $Y$ | $\ln(X)$ | X의 **상대적 변화** 시 Y의 절대적 변화 |
| Log-log | $\ln(Y)$ | $\ln(X)$ | **탄력성** (상대적 → 상대적) |

이제 정리하면: 회사 A처럼 "광고비 1% 증가 시 매출 몇 % 증가"를 알고 싶으면 **Log-log** 모형이 적합하다. 기울기가 곧 탄력성이다.

**만약 탄력성이 0.4라면?** 광고비 10% 증가 시 매출 4% 증가. 1보다 작으므로 수확체감. 탄력성이 1이면 정비례, 1보다 크면 수확체증.

**일상 상황으로 느끼기:** 커피를 마시면 집중력이 올라간다. 1잔 → 집중력 크게 상승(Log-lin). 2잔 → 조금 더 상승. 5잔 → 거의 안 올라감(수확체감). 10잔 → 오히려 떨어짐(비선형). "커피 1잔 추가 시 집중력 상승률"은 점점 줄어든다. 이것이 Log-lin 모형의 현실 직관이다.

**함수형태 선택의 실전 팁:**
- 종속변수가 "금액"이고 독립변수가 "수량"이면 → **Lin-lin**(기본)부터 시도
- "몇 % 변화하는가"가 관심사이면 → **Log 변환** 적용
- 잔차 그래프에 곡선 패턴이 보이면 → 로그 변환이 필요하다는 신호


### 왜 "상관관계 ≠ 인과관계"가 투자에서 중요한가?

**숫자로 느끼기:** 아이스크림 판매량과 익사 사고의 상관계수 r = 0.85. 회귀식: 익사 = 2 + 0.3 × 아이스크림. "아이스크림을 줄이면 익사가 줄어드나?" 아니다. 둘 다 "여름 기온"이라는 숨겨진 변수(교란변수)에 의해 움직인다.

**투자에서의 함정:** GDP 성장률과 주식 수익률의 회귀에서 $R^2 = 0.04$ (4%). 중국 GDP가 10년간 6~8% 성장했지만, 상하이지수는 2007년 고점을 미회복. GDP는 "과거 활동"을 측정하고, 주가는 "미래 기대"를 반영하기 때문이다.

**변수를 바꾸면?** "GDP 성장률"이 아닌 "GDP 서프라이즈(실제 - 예상)"로 바꾸면 $R^2$가 0.04 → 0.15로 올라갈 수 있다. 시장은 예상된 성장을 이미 반영했으므로, "예상과 실제의 차이"가 주가를 움직이는 진짜 변수다.


### 왜 잔차 분석이 "모형을 믿어도 되는지" 알려주는가?

**일상 상황:** 네비게이션이 "30분"이라 했는데 실제로 25, 35, 28, 45분. 잔차: -5, +5, -2, +15. 마지막 +15가 이상하다 — 사고가 있었을 것이다. 잔차에서 패턴이 보이면 모형이 뭔가를 놓치고 있다는 신호다.

**통행량-매출 잔차 패턴:**
- 통행량 100: 잔차 ±2 (작은 분산)
- 통행량 300: 잔차 ±6~8 (큰 분산)

통행량이 높을수록 잔차가 커지는 **부채꼴 패턴** = 이분산성. 통행량이 많은 날은 날씨, 행사 등 다른 요인이 복합적으로 작용해 예측이 부정확해진다. **변수를 바꾸면?** "날씨" 더미 변수를 추가하면 이분산성이 줄어들 수 있다 — 다중 회귀의 영역이다.


### R² 해석의 함정 — 숫자로 직접 확인

**왜 R²가 높다고 좋은 모형이 아닐 수 있는가?**

**시나리오 1 — 허위 상관:** 1990~2020년 니콜라스 케이지 영화 출연 수와 수영장 익사 사고 수의 R² = 0.87. 설명력 87%! 하지만 당연히 인과관계가 없다. 둘 다 시간에 따라 증가하는 추세가 있어서 높은 상관이 나온 것이다.

**시나리오 2 — 낮은 R²의 가치:** GDP 성장률과 주식 수익률의 R² = 0.04. 4%밖에 설명 못 한다. 하지만 이것이 유의하다면, GDP 예측이 가능한 투자자에게는 수익 창출 기회가 된다. 4%의 설명력이라도 수백만 원의 수익으로 이어질 수 있다.

**역으로 생각해봅시다.** R² = 1.00이면? 모든 점이 정확히 직선 위에 있다. 현실에서 이런 일은 거의 없다. R² = 1.00을 발견하면 오히려 의심해야 한다 — 종속변수와 독립변수가 정의상 같은 것을 측정하고 있을 가능성이 높다 (항등식).


## Module Quiz 10.3

**Q1.** Y=5+3.5X, $s_f$=2.5, X=10, 90% 신뢰구간:
$\hat{Y}=40$, $40 \pm 1.65(2.5) = 35.9 \sim 44.1$

**정답: C**

**Q2.** 독립변수의 상대적 변화 → 종속변수의 절대적 변화:
**정답: B. lin-log**


## 핵심 개념 정리 (Key Concepts)

### LOS 10.a
$\hat{b}_1 = \text{Cov}/\text{Var}$, $\hat{b}_0 = \bar{Y} - \hat{b}_1\bar{X}$

### LOS 10.b
4가지 가정: 선형, 등분산, 독립, 정규

### LOS 10.c~d
$R^2 = \text{SSR}/\text{SST}$, $F = \text{MSR}/\text{MSE}$, $t = \hat{b}_1/s_{\hat{b}_1}$ (df=n-2)
SEE = $\sqrt{\text{SSE}/(n-2)}$

### LOS 10.e
$\hat{Y} \pm t_c \times s_f$

### LOS 10.f
Log-lin(상대 변화율), Lin-log(절대 변화), Log-log(탄력성)
